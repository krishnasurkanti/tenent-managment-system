## Frontend Structure

This folder matches the target architecture shape:

- `app/`
- `components/`
- `lib/`
- `middleware.ts`

Current running Next.js source remains in `src/` for compatibility.
We can migrate execution to `frontend/` in a separate safe step.
