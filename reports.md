# Playwright E2E Test Reports

This file is appended by E2E run summaries (layout, accessibility, scroll/perf).

Run the layout tests:

```bash
npm run test:e2e:layout
```

Approve visual snapshot changes:

```bash
npm run test:e2e:visual-approve
```

The CI or test runner can append JSON summaries here when desired.
