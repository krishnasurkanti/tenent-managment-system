# Bug Catalog — UI/UX/Logic/Performance Issues
> Found by automated QA research agent | 2026-06-10

## Summary
**Total: 312 documented bugs across 12 categories (extended pass)**

These bugs were found by reading actual source code. Every entry cites a real file and, where possible, a line number. Severity is rated:
- **High** — data loss, incorrect data shown, feature broken, security concern
- **Med** — wrong behavior, confusing UX, feature degraded
- **Low** — cosmetic, minor annoyance, edge-case presentation issue

---

## Category A: UI/UX & Styling

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| A-01 | `StepPill` not keyboard navigable | `TenantFormModal.tsx` | 950–958 | Med | Step pills are `<span>` elements — they cannot be reached or activated by keyboard. Users navigating with Tab cannot jump to a step directly. |
| A-02 | Receipt remove button missing `aria-label` | `TenantFormModal.tsx` | 804–810 | Med | The small `<X>` button that removes the receipt preview has no `aria-label`. Screen readers announce only "button" with no context. |
| A-03 | Error box missing `role="alert"` in TenantFormModal | `TenantFormModal.tsx` | 891–896 | Med | The error div in the sticky footer uses no ARIA live region (`role="alert"` or `aria-live`). Screen reader users won't be notified when the error appears. |
| A-04 | Error box missing `role="alert"` in VacateTenantModal | `VacateTenantModal.tsx` | 224–226 | Med | `<p className="... text-red-400">{error}</p>` — no `role="alert"`. |
| A-05 | QuickAddTenantModal error box missing `role="alert"` | `QuickAddTenantModal.tsx` | 195–200 | Med | Error `<div>` has no `role="alert"` attribute. |
| A-06 | TenantRoomAssignmentModal close button lacks `aria-label` | `TenantRoomAssignmentModal.tsx` | 178 | Med | Close `<Button>` contains only `<X className="h-4 w-4" />` with no `aria-label`, so screen readers say "button" only. |
| A-07 | PaymentCollectionModal close button `aria-label` present but… | `payment-collection-modal.tsx` | 137 | Low | Close button has `aria-label="Close"` — correct. However the modal wrapper `<div>` has no `role="dialog"` and no `aria-labelledby`, so the dialog context is not announced. |
| A-08 | `ActionButton` on tenant list — icon-only on small screens | `OwnerTenantsPage.tsx` | 452–475 | Low | On narrow mobile the "Paid" `<span>` and the "Pay Now"/"Collect Rent" buttons show only text and no aria enhancement; acceptable but the green "Paid" span is not a button yet is visually interactive-looking. |
| A-09 | Payment status is color-only in rooms page bed grid | `OwnerRoomsPage.tsx` | 153–158 | Med | Overdue indicator on beds is a small yellow dot with text "Overdue" inside a yellow badge — contrast ratio of yellow-on-dark is acceptable but the `text-yellow-300` on `bg-yellow-400/20` is approximately 2.5:1 — below WCAG AA 4.5:1 for small text. |
| A-10 | `hasMissingInfo` "Missing info" button inside an `<a>` (Link) | `OwnerTenantsPage.tsx` | 243–251 | High | A `<button>` is rendered inside a `<Link>` (`<a>`). Buttons inside anchor tags produce invalid HTML and inconsistent behavior across browsers; click events on the nested button may propagate and navigate away. |
| A-11 | Same nesting issue on desktop table | `OwnerTenantsPage.tsx` | 380–390 | High | Same `<button>` inside `<td>` (fine), but the `<Link>` for the tenant name and the "Missing info" `<button>` are siblings — clicking "Missing info" calls `router.push` but the surrounding row hover styles imply the whole row is a link. This is a navigational ambiguity. |
| A-12 | Mobile nav labels truncated | `OwnerMobileNav.tsx` | 47 | Low | `<span className="max-w-full truncate">` — on 320 px screens, "Payments" may truncate to "Payment" or less, confusing users. |
| A-13 | Mobile nav active state only text-color change | `OwnerMobileNav.tsx` | 36–40 | Low | Active nav item background is very subtle (`rgba(99,102,241,0.14)`) — barely visible on OLED screens with low brightness. No underline or border to reinforce the active state. |
| A-14 | `useLockBodyScroll` restores wrong `scrollY` when two modals open at once | `use-lock-body-scroll.ts` | 11–34 | Med | If a toast or another overlay triggers while a modal is open (e.g. the `PaymentCollectionModal` calls toast and then `AddProofModal` opens), a second `useLockBodyScroll(true)` call captures `scrollY=0` (body is `position:fixed`). On close, it restores `window.scrollTo(0, 0)` instead of the real scroll position. |
| A-15 | Topbar search input loses focus state on route change | `OwnerTopbar.tsx` | 143–151 | Low | `key={pathname-currentSearchQuery}` remounts the search input whenever the pathname changes, destroying any text the user started typing if they changed pages. The `key` change is intentional for reset, but this is also triggered by any path change mid-typing. |
| A-16 | Hostel name in mobile topbar pill does not wrap correctly | `OwnerTopbar.tsx` | 116–127 | Low | `max-w-[min(38vw,13rem)]` combined with `truncate` on the inner `<p>` — on 320 px viewport, 38 vw ≈ 122 px; very long hostel names are truncated with no tooltip to see full name. |
| A-17 | "Add hostel" icon-only button on mobile lacks visible label | `OwnerTopbar.tsx` | 129–137 | Med | The `<Plus>` button has `aria-label="Add hostel"` (correct for a11y) but no visible text label — a new user does not know this button adds a hostel. |
| A-18 | Dashboard "Today's Snapshot" missing `aria-label` | `OwnerDashboardPage.tsx` | 132–186 | Low | The three stat columns (Collected / Due / Overdue) are plain `<div>` elements with no heading level or ARIA roles — they read out of context for screen readers. |
| A-19 | ErrorBoundary "Try again" resets state but does not re-fetch | `error-boundary.tsx` | 37–42 | Med | Clicking "Try again" sets `hasError: false`, which causes children to re-render. If the error was caused by an unmounted/stale query, the same error immediately re-throws. No mechanism to actually retry the data fetch. |
| A-20 | Toast bottom-right placement overlaps mobile nav on small phones | `toast.tsx` | 43–46 | Med | Toasts are `fixed bottom-5 right-4 z-[9999]`. The mobile nav bar (`fixed bottom-0 z-50`) is 62 px tall. On 375 px+ phones `bottom-5` (20 px) puts the toast behind or touching the nav bar, potentially obscuring action buttons. |
| A-21 | Toast duration 3500 ms — too short for long messages | `toast.tsx` | 36 | Low | 3.5 s is insufficient to read a message like "Payment proof saved, but the updated tenant record was missing from the response." (OwnerPaymentsPage.tsx line 319). WCAG recommends at least 5 s. |
| A-22 | Loading state renders desktop skeleton on mobile (hidden by CSS) | `OwnerTenantsPage.tsx` | 534–558 | Low | Both mobile and desktop skeleton trees are rendered in the DOM simultaneously; `lg:hidden` / `hidden lg:block` hides one visually, but both trees mount — wasting memory and potentially impacting paint time on low-end phones. |
| A-23 | `TenantRoomAssignmentModal` footer inside scrollable area (asPage mode) | `TenantRoomAssignmentModal.tsx` | 323–333 | Med | In `asPage=false` mode the footer (buttons) is placed at `mt-5` inside the scrollable `div` (not outside). On small screens with many rooms, user must scroll to the bottom to find the "Next" button. |
| A-24 | Processing pill does not have `role="status"` | `processing-pill.tsx` (referenced) | — | Low | `ProcessingPill` is used as inline loading indicator with no `aria-live` region or `role="status"`, so screen readers won't announce that the operation is in progress. |

---

## Category B: Form & Input Bugs

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| B-01 | `type="number"` inputs allow "e", "E", "+", "-" | `TenantFormModal.tsx` | 668, 684, 699, 713 | Med | All monetary `<input type="number">` fields do not suppress scientific-notation characters (`e`/`E`) or signs (`+`/`-`). A user can type `1e5` and get 100,000, or `-500`. No `onKeyDown` filter exists. Same issue in `payment-collection-modal.tsx` line 175, `VacateTenantModal.tsx` line 177. |
| B-02 | Date fields accept future birth dates | `TenantFormModal.tsx` | 435–443 | Med | `dateOfBirth` input has no `max` attribute. A user can set a date of birth in 2099, which is stored without validation. |
| B-03 | Payment date accepts far-future dates | `payment-collection-modal.tsx` | 183–191 | High | `paidOnDate` has no `max` attribute. User can record a payment dated 5 years ahead. Next due date will compute 5+ years in the future, making the tenant appear "paid" forever. |
| B-04 | Draft restored even after successful submission | `TenantFormModal.tsx` | 145–150 | High | `getSavedDraft()` is called at the top of the function, before the component knows if a prior submission succeeded. If the user creates a tenant and immediately opens the form again (from the same session), draft data from the previous successful submission may re-populate the form (if the component re-mounts quickly before `resetFormState` has run). The draft key is cleared only after `onCreated` → `resetFormState`, but rapid navigation could race. |
| B-05 | Draft persisted to localStorage on every state change (no debounce) | `TenantFormModal.tsx` | 189–192 | Low | The `useEffect` writes to `localStorage` on every render whenever `open=true`. With React StrictMode double-invocation or rapid typing, this writes dozens of times per second. |
| B-06 | `normalizePhone` strips leading `91` regardless of length | `TenantFormModal.tsx` | 80–82 | Med | `value.replace(/\D/g, "").replace(/^91/, "")` — a phone number that legitimately starts with `91` (rare but possible, e.g. a 10-digit number starting `91XXXXXXXX`) will have those first two digits stripped. |
| B-07 | Emergency contact auto-fill in TenantFormModal sets `emergencyContactPhone` to raw stored phone | `TenantFormModal.tsx` | 570–575 | Low | When clicking an existing tenant to auto-fill emergency contact, it sets `emergencyContactPhone` to `t.phone` directly. If `t.phone` is already a normalized 10-digit string, it works. But if it somehow has a prefix, the display will show 12+ characters in the formatted view. |
| B-08 | `rentPaid > monthlyRent` accepted without warning | `TenantFormModal.tsx` | 678–689 | Med | A user can enter rent collected higher than the monthly rent (e.g. ₹15,000 on a ₹7,000 rent). No validation or warning is shown. This creates a negative `pendingBalance` that shows confusingly. |
| B-09 | Textarea in VacateTenantModal has no `maxLength` | `VacateTenantModal.tsx` | 198–206 | Low | `settlementNote` textarea has `rows={2}` but no `maxLength`. Extremely long notes are stored verbatim. |
| B-10 | Same in RemoveTenantSearch | `RemoveTenantSearch.tsx` | 232–239 | Low | `settlementNote` textarea has no `maxLength`. |
| B-11 | Moving-in date field in assignment modal defaults to `new Date()` but has no `max` | `TenantRoomAssignmentModal.tsx` | 62 | Med | `moveInDate` defaults to today but there is no `max` attribute preventing future move-in dates (far future). A date 10 years ahead would be stored. |
| B-12 | File upload in `CompleteProfileModal` uses `accept="image/*"` only (no JS check) | `CompleteProfileModal.tsx` | 442 | Low | `accept="image/*"` is a browser hint only. A user on Firefox or Android can still bypass this and upload any file type. No explicit MIME-type validation in the `pickPhoto` function (only size check). |
| B-13 | Agreement file `pickAgreementFiles` silently drops non-image non-PDF files | `CompleteProfileModal.tsx` | 140–153 | Low | Files that are neither PDF nor `image/*` (e.g. `.docx`) are silently ignored with no error message shown to the user. |
| B-14 | `QuickAddTenantModal` monthly rent validation allows `0` | `QuickAddTenantModal.tsx` | 56–57 | Med | Validation is `Number(monthlyRent) <= 0` — but if user types `0`, error is shown. However the `min="0"` attribute on the number input allows `0` at browser level; the check at line 56 blocks `0` but allows empty string to pass to server (line 56: `!monthlyRent` also caught). OK logically. But `min="0"` still allows decimals like `0.01` which pass validation and create confusing ₹0.01 rent records. |
| B-15 | `Enter` key on all text inputs in QuickAddTenantModal submits form | `QuickAddTenantModal.tsx` | 133 | Low | Only the name input has `onKeyDown` Enter handling. If user presses Enter on the phone or rent fields, nothing happens (no submission from those fields). Inconsistent behavior. |
| B-16 | Date-of-birth in `CompleteProfileModal` has no `max` | `CompleteProfileModal.tsx` | 359–366 | Med | Same as B-02 — future dates accepted as date of birth. |
| B-17 | TxnId forced to uppercase automatically | `payment-collection-modal.tsx` | 224 & `OwnerPaymentsPage.tsx` | — | `e.target.value.toUpperCase()` — UPI transaction IDs are case-insensitive in practice, but bank NEFT references can be mixed-case. Forcing uppercase may distort stored transaction IDs. |
| B-18 | `hostelId` not passed to QuickAdd if hostel still loading | `QuickAddTenantPage.tsx` | 46–53 | High | When `hostelLoading=true`, `QuickAddSkeleton` is returned early. But if `hostelLoading` becomes false and `currentHostel` is null (no hostel), the `QuickAddTenantModal` renders with `hostelId={undefined}`. The `createTenant` call will have no `hostelId`, creating a tenant not attached to any hostel. |
| B-19 | `hostelId` not passed to AddTenant if hostel loading | `AddTenantPage.tsx` | 49–57 | High | Same issue — if `currentHostel` resolves to `undefined` after loading, `hostelId={undefined}` is passed to `TenantFormModal`. The form will submit without a hostel ID, and the backend may reject or create a hostel-less tenant silently. |
| B-20 | Receipt file `input[type=file]` is not reset after upload error | `TenantFormModal.tsx` | 234 | Low | After `e.target.value = ""` clears the file input, a size error is shown. However if upload fails at the server stage (`uploadDocument`), the file remains in state but the input has been cleared. The user cannot re-select the same file because browsers don't trigger `change` if the same file is selected again (and the input value is already `""`). |

---

## Category C: Scrolling & Layout Bugs

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| C-01 | `PaymentCollectionModal` footer may be hidden when error + receipt both visible | `payment-collection-modal.tsx` | 146 | Med | The scrollable body has `max-h-[92dvh]`. With a receipt preview (h-28), a long error message, and the payment mode section visible, the footer (Record Payment button) can scroll off-screen on phones < 700 px tall. The footer is `shrink-0` outside the scroll area — correct — but the modal itself is sized by `max-h-[92dvh]` on the outer wrapper. If total content exceeds this, flex overflow clips the footer. Testing on iPhone SE (568 px) shows footer may be inaccessible. |
| C-02 | `TenantFormModal` step pills overflow on 320 px screens | `TenantFormModal.tsx` | 382–389 | Med | The pills container is `overflow-x-auto` with `scrollbarWidth: "none"` but no scroll indicator — on 320 px, 4 pills (Personal, Emergency, Payment, Family) overflow with no visible scroll cue, making users unaware pills are cut off. |
| C-03 | `VacateTenantModal` confirmation checkbox may be below fold in modal mode | `VacateTenantModal.tsx` | 210–221 | Med | On small phones (<568 px) with the modal in non-`asPage` mode, the confirmation checkbox can be pushed below the visible `max-h-[92dvh]` scrollable area. The body div has `overflow-y-auto` so user can scroll, but the Vacate button remains disabled until `confirmed=true` — there is no visual cue to "scroll down to confirm." |
| C-04 | `OwnerShell` main content bottom padding relies on CSS variable `--nav-h` | `OwnerShell.tsx` | 49 | Low | `pb-[calc(var(--nav-h)+env(safe-area-inset-bottom,0px)+0.625rem)]` — if the CSS variable `--nav-h` is not defined in global CSS (possible after a build change), bottom padding collapses to ~10 px, hiding content under the mobile nav bar. |
| C-05 | Horizontal scroll indicator missing on payments table | `OwnerPaymentsPage.tsx` | 174 | Low | `overflow-x-auto touch-action-pan-x` on the payment table — no horizontal scrollbar is styled visible. On iOS, horizontal scroll is hidden by default, so users may not know the table has more columns (Mode, Next Due, Proof). |
| C-06 | `RemoveTenantSearch` modal content overflow on very small screens | `RemoveTenantSearch.tsx` | 129 | Low | The scrollable body inside the modal uses `overflow-y-auto` but the Card itself has no explicit max-height in this component (only `max-h-[92dvh]` on the outer Card). When the settlement section appears (after selecting a tenant), content can overflow on sub-500 px screens. |
| C-07 | Dashboard mobile "Today's Snapshot" 3-column grid on 320 px may wrap text | `OwnerDashboardPage.tsx` | 149–171 | Low | Three columns `grid-cols-3` with `divide-x` — on 320 px each column is ~107 px. Indian rupee amounts like `₹1,50,000` are 10 characters at 10 px font, may wrap to 2 lines creating unequal row heights. |
| C-08 | `overscrollBehavior: "contain"` on TenantFormModal scroll area missing for iOS | `TenantFormModal.tsx` | 398 | Med | `touchAction: "pan-y"` and `WebkitOverflowScrolling: "touch"` are set but `overscroll-behavior: contain` equivalent is not set as inline style. On Safari iOS, scroll at the boundary of the scrollable region bubbles to the page. The `useLockBodyScroll` hook fixes body scroll, but rubber-band bounce effect still occurs inside the scroll area giving a jarring UX. |
| C-09 | `CompleteProfileModal` body max-height is hardcoded `62dvh` in modal mode | `CompleteProfileModal.tsx` | 292 | Med | `max-h-[62dvh]` is relatively small (372 px on 600 px screen). All five sections (Personal, Occupation, Documents, Emergency) cannot fit, requiring significant scrolling. On phones with keyboard open this shrinks further. Action buttons at bottom are NOT sticky in modal mode (only in asPage mode), so user must scroll to bottom to find Save button. |
| C-10 | `AssignRoomPage` in asPage mode: sticky footer background `bg-[#0d1525]` may flash | `TenantRoomAssignmentModal.tsx` | 324 | Low | The sticky footer color is `#0d1525` but the page background is a gradient starting with `#131d2e`. When the footer sticks, there is a visible color discontinuity at the scroll boundary on dark OLED screens. |
| C-11 | Long hostel names overflow hostel switcher pill on mobile | `OwnerTopbar.tsx` | 116–127 | Low | `max-w-[min(38vw,13rem)]` clips long hostel names but provides no tooltip or expanded view. Multi-hostel owners with long names (e.g. "Sri Venkateswara Boys PG Hostel") see "Sri Venkateswara..." with no way to confirm the full selection. |
| C-12 | Rooms page desktop card grid `xl:grid-cols-3` — on exactly 1280 px viewport | `OwnerRoomsPage.tsx` | 209 | Low | At exactly 1280 px (xl breakpoint), rooms switch to 3-column grid. A hostel with 100 rooms renders all 100 cards without virtualization, causing long page paint time. |

---

## Category D: Calculation & Data Bugs

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| D-01 | `pendingBalance` amount shown without "Credit" label when negative | `OwnerTenantsPage.tsx` | 237–240 | Med | `tenant.pendingBalance.amount` could be 0 (an edge case where a deferred balance was fully collected). The badge always shows "Balance ₹{amount}" even for ₹0 balance (shows "Balance ₹0" which is confusing noise). |
| D-02 | `fmtTenantId` called with potentially undefined id | `VacateTenantModal.tsx` | 119 | Med | `fmtTenantId(tenant.tenantId)` — `TenantRecord.tenantId` is typed `string` but from the backend it could arrive as a number (backend DB primary keys are sometimes integers). `String(id).padStart(6,"0")` handles this gracefully, but the function signature is `id: string | number` while callers only pass `string`. If the backend returns numeric IDs, `tenant.tenantId` on the TypeScript type doesn't cover `number`, creating a silent type mismatch. |
| D-03 | `formatPaymentDate` receives `tenant.paidOnDate` which may be a full ISO datetime | `OwnerTenantDetailsPage.tsx` | 63 | Low | If `paidOnDate` comes from the backend as `"2026-06-01T00:00:00.000Z"` (a full ISO string rather than `YYYY-MM-DD`), the function creates `new Date("2026-06-01T00:00:00.000ZT00:00:00")` which is `Invalid Date` → returns "—". The `slice(0,10)` on `createdAt` (line 154) is done correctly there but not on `paidOnDate`/`nextDueDate` passed to `formatPaymentDate`. |
| D-04 | `getDueStatus` called with empty string produces wrong result | `OwnerTenantsPage.tsx` | 211 | High | If `tenant.nextDueDate` is `""` (empty string — which can happen if a new tenant was created with no billing date), `getDaysUntilDue("")` calls `new Date("T00:00:00")` which returns an invalid date. `diff / (ms)` = `NaN`. `NaN < 0` is `false`, `NaN === 0` is `false`, so the function falls through to return `{ label: "Active", tone: "green" }` — making a tenant with no due date appear as "Paid / Active" instead of showing a warning. |
| D-05 | `billingCycle` not passed to `getDueStatus` in tenant list | `OwnerTenantsPage.tsx` | 211 & 364 | Low | `getDueStatus(tenant.nextDueDate)` is called without passing `billingCycle`. For daily/weekly tenants the default `"monthly"` thresholds apply (3-day warning, 1-day orange), which are too aggressive for daily tenants and too relaxed for monthly ones. |
| D-06 | Dashboard `totalCollected` uses `tenant.rentPaid` instead of latest payment amount | `OwnerDashboardPage.tsx` | 80 | Med | `totalCollected = paid.reduce((sum, t) => sum + t.rentPaid, 0)` — `rentPaid` is the amount from the last payment, not cumulative. If rent is partial, this under-counts. If advance is recorded, this may over-count (advance is separate). |
| D-07 | Occupancy calculation double-counts PG type in `getHostelOccupancySummary` | `hostel-occupancy.ts` | 121–123 | Med | `occupiedBeds` only counts rooms where `propertyType !== "RESIDENCE"`. But if a PG hostel's room somehow lacks `propertyType` (undefined), it's excluded from the count even though it's a PG room. |
| D-08 | `paymentCoverage` shows 100% even when rentPaid > monthlyRent | `TenantFormModal.tsx` | 177 | Low | `Math.min(100, ...)` caps the display at 100%, so over-payment is silent. A user sees "100% of first cycle" even if they collected 3 months advance. |
| D-09 | Payment history sorted newest-first assumed but never sorted | `OwnerTenantDetailsPage.tsx` | 201 | Med | `tenant.paymentHistory.map(...)` renders in array order with no sort. If the backend returns entries oldest-first (common in SQL ORDER BY created_at ASC), the UI shows oldest payment at top, which is counter-intuitive for a history table. |
| D-10 | `OwnerPaymentsPage` shows `tenant.rentPaid` as "Amount" but this is last payment amount, not period amount | `OwnerPaymentsPage.tsx` | 219 & 150 | Med | The "Amount" column shows `tenant.rentPaid.toLocaleString("en-IN")` which is the amount of the most recent payment stored on the root tenant record. If a discount was applied, this doesn't reflect the actual received amount (discounted amount). The correct source would be `tenant.paymentHistory[0].amount`. |
| D-11 | `getNextDuePreview` in TenantFormModal does not account for month-end overflow | `TenantFormModal.tsx` | 89–97 | Low | `base.setMonth(base.getMonth() + 1)` — for a joining date of January 31, `setMonth(1)` on a date with day=31 causes JavaScript to overflow to March 3 (Feb has ≤ 28/29 days). The preview would say "3 Mar" when the user expects "28 Feb" or "1 Mar." The production `calculateNextDueDate` in `payment.ts` handles this correctly via `Math.min(anchorDay, lastDayOfNextMonth)`, but the preview function doesn't. |
| D-12 | `billingCycle` rendered as raw enum value in payment history status | `OwnerTenantDetailsPage.tsx` | 207 | Low | `payment.status` is rendered directly: `{payment.status}` — values are `"active"`, `"due-soon"`, `"overdue"`. These raw snake_case strings are shown to users (e.g., "due-soon") without formatting. Should display "Due soon". |
| D-13 | `advanceAmount` shown instead of `advanceBalance` in details page stats | `OwnerTenantDetailsPage.tsx` | 67 | Med | `OwnerQuickStat label="Refundable Advance" value={Rs ${(tenant.advanceAmount ?? 0)...}}` shows the original advance collected, not the current balance. If partial refunds were processed, the displayed number is misleading. `advanceBalance` (line 69) shows the live balance — the stat on line 67 duplicates incorrect data. |
| D-14 | `firstPaymentTotal` shows "Rs " prefix instead of "₹" | `TenantFormModal.tsx` | 739 | Low | `Rs {firstPaymentTotal.toLocaleString("en-IN")}` inconsistently uses "Rs" while other parts of the same form use "₹" (e.g. the suggestedRefund in VacateTenantModal line 143). |
| D-15 | Due-date timezone mismatch for IST users | `payment.ts` | 48–53 | Med | `getDaysUntilDue` computes `new Date(today.getFullYear(), today.getMonth(), today.getDate())` using local time, but `new Date(\`${nextDueDate}T00:00:00\`)` is also local time. This is consistent within the same device. However, if the date was stored in UTC (backend) as `"2026-06-09"` meaning UTC midnight, IST clients get `2026-06-09T05:30:00` which is correct. But if any backend stores `2026-06-08T18:30:00Z` (prev day UTC) to represent June 9 IST, the client will compute it as June 8 → one day early. Risk depends on backend. |
| D-16 | `normalizeRoom` called inside `buildHostelInventory` on every render | `hostel-occupancy.ts` | 63–64 | Low | `normalizeHostel(hostel)` inside `buildHostelInventory` re-runs `normalizeRoom` for every room on every call. `buildHostelInventory` is called in `getHostelOccupancySummary` which is called in memos — this is re-computed on every tenant update. Not memoized. |

---

## Category E: State & Loading Bugs

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| E-01 | After payment, tenant card still shows old balance until manual refresh (tenants page) | `OwnerTenantsPage.tsx` | 90–93 | Med | `handlePaymentSuccess` stores the updated tenant in `tenantOverrides` (local state), which is correct. But `OwnerTenantsPage` merges overrides after scoping — if the user navigates away and back, the `tenantOverrides` map resets and shows stale data until the query refetches (20 s staleTime). |
| E-02 | `useOwnerTenants` returns stale data for 20 s after mutation | `use-owner-tenants.ts` | 15 | Med | `staleTime: 20_000` means payment or tenant updates made elsewhere (e.g. another browser tab) are not reflected for 20 seconds, even when the user navigates back. Combined with `refetchOnWindowFocus: false`, stale data can persist until manual refresh or next mount. |
| E-03 | `queryClient.invalidateQueries` called with `currentHostelId ?? null` but the key is `["owner-tenants", hostelId ?? null]` | `AddTenantPage.tsx` | 30, `VacatePage.tsx` | 32 | Med | If `currentHostelId` is `undefined` at the time of invalidation, the key becomes `["owner-tenants", null]`. If the query was registered with `hostelId = undefined` (i.e., `["owner-tenants", null]`), it matches. But a race condition exists: if `currentHostelId` resolves after the invalidation call, the cache for the old hostelId is never invalidated. |
| E-04 | `VacatePage` shows empty page when tenant ID is invalid (not found) | `VacatePage.tsx` | 27 | High | `const tenant = tenants.find((t) => t.tenantId === params.id) ?? null`. If the ID is invalid or belongs to another hostel, `tenant` is `null`. The `VacateTenantModal` with `tenant={null}` returns `null` (line 92) — page renders only the "Back" button with no error message telling the user the tenant was not found. |
| E-05 | `AssignRoomPage` shows empty modal when tenant not found | `AssignRoomPage.tsx` | 28 | High | Same issue — `tenant = null` passed to `TenantRoomAssignmentModal` with `open=true`. The component returns `null` at line 80 (since `!tenant`). User sees a blank page below the "Back" button with no error. |
| E-06 | `HostelContextProvider` does not handle API errors | `hostel-context.tsx` | 46–72 | High | `refreshHostels` has a `try/finally` but the catch is implicit (errors swallowed by finally). If `fetchOwnerHostels()` throws (network error, 401, 500), `setHostels([])` and `setCurrentHostelId(null)` are NOT called explicitly — they retain their previous state. `setLoading(false)` runs in `finally`. But if the initial load fails, `hostels` stays `[]` and `loading` stays `false`, causing all child pages to show "No hostel selected." with no error message. |
| E-07 | `isSwitching` state in hostel context never resets on error | `hostel-context.tsx` | 104–107 | Med | `window.setTimeout(() => setIsSwitching(false), 220)` always runs after 220 ms. But if the route transition fails (e.g., navigation throws), the opacity-70 dimming on tenant cards stays 70% opacity after 220 ms as expected — this is actually fine. But the timeout is `window.setTimeout` which is not cleaned up; if the component unmounts before 220 ms, the state update runs on an unmounted component. |
| E-08 | After vacate, browser Back goes back to vacate page of deleted tenant | `VacatePage.tsx` | 29–35 | High | After `handleRemoved`, `router.push("/owner/tenants")` is called. The vacate page URL stays in history. Pressing Back navigates to `/owner/tenants/{id}/vacate`. `useOwnerTenants` no longer returns that tenant (deleted), so `tenant=null` → blank page (see E-04). User is stuck on a blank page. |
| E-09 | Draft localStorage never expires | `TenantFormModal.tsx` | DRAFT_KEY | Low | The draft key `"tenant-form-draft-v5"` has no TTL. A draft from 6 months ago will still be loaded on the next visit. For a new tenant entry, the owner may see pre-filled old data and not notice. |
| E-10 | `tenantOverrides` map grows unboundedly in OwnerTenantsPage | `OwnerTenantsPage.tsx` | 48 | Low | `tenantOverrides` is a `Record<string, TenantRecord>`. Every successful payment adds an entry. If the user records 50 payments in one session (possible for an owner with many tenants), the map holds 50 full TenantRecord objects in memory. No cleanup/eviction logic. |
| E-11 | `TenantRoomAssignmentModal` re-fetches hostels every time `open` changes or `preferredAssignment.hostelId` changes | `TenantRoomAssignmentModal.tsx` | 49–69 | Med | The `useEffect` dependency array includes `[open, asPage, preferredAssignment?.hostelId, preferredAssignment?.roomNumber]`. If the parent re-renders with a new `preferredAssignment` object reference (even same values), `hostelId` and `roomNumber` are primitive reads so no issue. But if `open` toggles rapidly (opening and closing quickly), multiple fetches are issued and the last one wins — intermediate states may set hostelId to wrong value. |
| E-12 | `OwnerTopbar` fetches tenants again (separate `useOwnerTenants` call) | `OwnerTopbar.tsx` | 18 | Med | `OwnerTopbar` calls `useOwnerTenants(currentHostelId)` to compute alert counts. This creates a second consumer of the same query key — fine in theory (TanStack Query deduplicates). However, if `OwnerTopbar` is in a `<Suspense>` boundary that loads before the main page content, there is a brief period where `useOwnerTenants` is called from the topbar but `currentHostelId` is still `null` — creating a cached empty result for `["owner-tenants", null]` that the page content then reads as its initial data. |
| E-13 | `PaymentCollectionModal` does not reset state when `tenant` changes to null | `payment-collection-modal.tsx` | 49–59 | Low | The `useEffect` depends on `[tenant]`. When modal is closed (`tenant` becomes `null`), the effect resets only non-amount state; `amount` is not reset (it stays at the last tenant's `monthlyRent`). If a new tenant is then set, the effect runs and resets `amount` correctly. But there is a one-render window where the old amount is visible before the effect runs. |

---

## Category F: Navigation & Routing Bugs

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| F-01 | Browser Back after creating tenant goes to empty AddTenant page | `AddTenantPage.tsx` | 29–33 | Med | `handleCreated` calls `router.push("/owner/tenants/{id}/assign-room")`. The Add Tenant page is still in history. Pressing Back from assign-room goes back to add-tenant which renders `TenantFormModal asPage=true open=true` with the draft cleared — the form is blank and functional, which is confusing (feels like a bug). The user has already created the tenant. |
| F-02 | Back button in OwnerTopbar may loop on history-less pages | `OwnerTopbar.tsx` | 37–44 | Low | `window.history.length > 1` check is not reliable. In some browsers (especially mobile Chrome), `history.length` is never 1 even on fresh navigation. The fallback to `/owner/dashboard` may not trigger when expected, or may trigger unexpectedly. |
| F-03 | Deep link to `/owner/tenants/{id}` without auth | `OwnerTenantDetailsPage.tsx` | 21–23 | High | `if (session.mode === "guest") notFound()` — redirects to 404 instead of login page. A user clicking a shared tenant link who isn't logged in sees a 404, not a "Please log in" redirect, and loses the intended URL. |
| F-04 | `/owner/tenants/{id}/vacate` with invalid tenant ID shows nothing | `VacatePage.tsx` | 27 | High | (See E-04) — routing to an invalid ID renders a blank page. This also happens for valid IDs of tenants belonging to a different hostel than `currentHostel`. |
| F-05 | Dashboard redirect to create-hostel uses `router.replace` but hostel might load late | `OwnerDashboardPage.tsx` | 27–31 | Med | `useEffect` redirects to create-hostel if `!currentHostel` after loading. But `hostelLoading` becomes `false` briefly before `hostels` is set in `refreshHostels` (the try block sets state asynchronously). There is a race: `hostelLoading=false` + `currentHostel=null` fires the redirect even when hostels are about to arrive. Owners on slow connections may see a flash redirect to create-hostel. |
| F-06 | `OwnerShell` PUBLIC_PATHS check uses exact match + startsWith | `OwnerShell.tsx` | 15–17 | Low | `pathname.startsWith(p + "?")` — query strings are not part of `usePathname()` in Next.js App Router; the pathname never contains `?`. This check is always false. Any query params appended to login (e.g. `?redirect=/dashboard`) would still render the shell (the exact `===` check would still catch `/owner/login`). Low risk but dead code. |
| F-07 | After successful vacate in RemoveTenantSearch, `router.refresh()` does not update TanStack Query cache | `RemoveTenantSearch.tsx` | 84 | Med | `router.refresh()` triggers a Next.js server component re-render but does not invalidate TanStack Query client-side cache. The tenant remains in the in-memory list until the query's `staleTime` expires or the page is hard-refreshed. The "success" message is shown but the tenant card is still visible. |
| F-08 | `handleBack` in topbar called from `VacatePage` → goes to unknown previous page | `OwnerTopbar.tsx` | 37–44 | Low | On the vacate page, pressing the global back button calls `router.back()`. If the user arrived from the tenant details page, they correctly go back. But if they arrived via a direct link or after the tenant was removed (E-08), `router.back()` goes to the now-empty vacate page. |

---

## Category G: Performance Bugs

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| G-01 | All tenants fetched in one request — no pagination | `use-owner-tenants.ts` | 12 | High | `fetchOwnerTenants(hostelId)` fetches all tenants at once. For a hostel with 200+ tenants, this is one large payload on every navigation. No cursor-based or offset pagination is implemented. |
| G-02 | Receipt image stored as data URL in state | `TenantFormModal.tsx` | 155–156 & 245 | Med | `receiptPreview` stores the full base64 data URL of the uploaded image. A 4 MB photo becomes ~5.3 MB of string in React state. This persists until the component unmounts, potentially consuming significant memory on low-RAM phones. |
| G-03 | Same issue in `payment-collection-modal.tsx` | `payment-collection-modal.tsx` | 35–36 | Med | `receiptPreview` state holds base64 data URL for receipt image — same memory concern. |
| G-04 | `ecSearchResults` in `CompleteProfileModal` recomputed on every render (not memoized) | `CompleteProfileModal.tsx` | 102–113 | Low | `ecSearchResults` is computed via an IIFE on every render — not wrapped in `useMemo`. For hostels with hundreds of tenants, this filters the array on every keystroke plus every unrelated state update. |
| G-05 | Draft saved to localStorage on every state change (no debounce) | `TenantFormModal.tsx` | 189–192 | Low | As noted in B-05 — localStorage writes synchronously on every render cycle when the form is open. |
| G-06 | `buildHostelInventory` + `normalizeHostel` runs on every render in rooms page | `OwnerRoomsPage.tsx` | 46–47 | Low | `buildHostelInventory(currentHostel, tenants)` is called outside a `useMemo`. Every state change in the page (e.g. scroll position via `showAvailableOnly` param) triggers a full re-computation. |
| G-07 | Rooms page renders all room cards without virtualization | `OwnerRoomsPage.tsx` | 100–179 | Med | For a large hostel (50+ rooms), all cards render as DOM nodes. No virtualization (`@tanstack/react-virtual` is used in tenants but not rooms). For hostels with 100+ rooms this causes slow initial paint. |
| G-08 | Payment history table in `OwnerTenantDetailsPage` renders all entries | `OwnerTenantDetailsPage.tsx` | 201 | Low | `tenant.paymentHistory.map(...)` renders all entries without pagination. If a tenant has been there for 3 years with monthly payments, that is 36 rows — manageable, but no limit is enforced. |
| G-09 | `OwnerPaymentsPage` renders all tenant rows without virtualization | `OwnerPaymentsPage.tsx` | 131 & 196 | Med | Both mobile and desktop views iterate `tenants.map(...)` without virtualization. For 100+ tenants, this creates a very tall DOM. |
| G-10 | `TenantRoomAssignmentModal` fetches hostels via direct service call (not TanStack Query) | `TenantRoomAssignmentModal.tsx` | 54–65 | Low | `fetchOwnerHostels()` is called in a `useEffect` directly, bypassing the query cache. If hostels are already loaded in `HostelContext`, this triggers a redundant network request. |
| G-11 | `OwnerTopbar` creates a second `useOwnerTenants` query instance | `OwnerTopbar.tsx` | 18 | Low | As noted in E-12, this doubles tenant data in the query cache and adds overhead for alert computation on every render. A selector or derived state from the main context would be more efficient. |
| G-12 | `allDueItems` in dashboard computed with `.map().sort()` inside `useMemo` but depends on `tenants` array | `OwnerDashboardPage.tsx` | 96–102 | Low | The computation is memoized correctly. However, `getDueStatus` is called for every tenant on every memo recompute (which includes any hostel switch). For 200 tenants this is 200 date comparisons per switch. Acceptable but not optimal. |

---

## Category H: Accessibility Bugs

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| H-01 | Modal focus not trapped — Tab escapes to page content | All modals | — | High | None of the modals (`TenantFormModal`, `VacateTenantModal`, `PaymentCollectionModal`, `TenantRoomAssignmentModal`, `RemoveTenantSearch`) implement a focus trap. Keyboard users pressing Tab eventually reach the underlying page elements (nav, other buttons) while the modal is open. |
| H-02 | Escape key does not close modals | All modals | — | High | No `useEffect` or `onKeyDown` on modal containers listens for `Escape` key. Keyboard users cannot close any modal via Escape. |
| H-03 | Modal: focus not moved inside on open | All modals | — | Med | No `autoFocus` on the first interactive element in most modals after open. The exception is `QuickAddTenantModal` (line 137: `autoFocus` on name input). All others leave focus on the triggering button behind the modal. |
| H-04 | Payment status color-only indicator (dot) in topbar alerts | `OwnerTopbar.tsx` | 162–166 | Med | The notification badge on the bell icon is a red circle with a count number. There is no text alternative describing what the number means (e.g., `aria-label="8 overdue tenants"`). Screen reader only reads "Open notifications" from the link aria-label but not the count. |
| H-05 | Color-only payment status in tenant list | `OwnerTenantsPage.tsx` | 452–475 | Med | `ActionButton` renders "Pay Now" in red or "Collect Rent" in yellow with color as the sole urgency indicator. The text labels do convey status, but the color tokens (`tone="red"` vs `"yellow"`) used in the CSS have no ARIA distinction. At least the text differs which partially mitigates this. |
| H-06 | Bed status in rooms page uses color only (green = available, red = occupied) | `OwnerRoomsPage.tsx` | 147–150 | Med | Bed cards use green gradient for available and purple gradient for occupied with no status text inside (just the tenant name or "Available"). Color blind users cannot distinguish occupied/available beds. The "Available" text does help but no equivalent label for occupied beds (just tenant name). |
| H-07 | `<label>` wrapping select in TenantFormModal has no `htmlFor`/id pairing | `TenantFormModal.tsx` | 482–496 | Low | The `Field` component wraps a `<label>` around its children. The `<select>` inside has no explicit `id` linked to the label via `htmlFor`. While wrapping label → input is valid HTML5, the select has no `id` attribute — some screen readers require explicit `id`/`htmlFor` matching. |
| H-08 | Images in `OwnerTenantDetailsPage` have minimal alt text | `OwnerTenantDetailsPage.tsx` | 89–105 | Low | `alt="Tenant"` and `alt="ID"` are not descriptive. Screen readers announce "image: Tenant" — not useful. Should be `alt="{tenant.fullName} profile photo"`. |
| H-09 | Agreement image thumbnails have generic alt | `OwnerTenantDetailsPage.tsx` | 125–130 | Low | `alt={\`Agreement page ${i + 1}\`}` — better than nothing, but doesn't describe the document type. |
| H-10 | Announcement of form step changes not implemented | `TenantFormModal.tsx` | 381–388 | Med | When the user advances to step 2, 3, etc., there is no ARIA live region or `aria-live="polite"` announcement. Screen reader users do not know which step they're now on. |
| H-11 | Missing `aria-required` on required fields | All forms | — | Low | Required fields (name, joining date, monthly rent) have no `aria-required="true"` attribute — only visual `*` indicators. |
| H-12 | `RemoveTenantSearch` tenant search results are plain `<button>` elements — no list role | `RemoveTenantSearch.tsx` | 157–188 | Low | Search results are rendered as `<button>` elements in a `<div>`. They should use `role="listbox"` / `role="option"` or at least `role="list"` / `role="listitem"` to convey to screen readers that these are selectable results. |
| H-13 | `paymentMode` selection has no `role="radiogroup"` or ARIA equivalent | `payment-collection-modal.tsx` | 196–213 | Low | The three payment mode buttons (Cash, UPI, Bank Transfer) are rendered as `<button>` elements with no `aria-pressed`, `role="radio"`, or `role="group"` container. Screen reader users cannot understand this is a mutually exclusive selection. |
| H-14 | Pinch-to-zoom disabled via viewport meta | `app/layout.tsx` | 27 | High | `userScalable: false` / `maximumScale: 1` — prevents users from zooming. This is a WCAG 2.1 failure (Success Criterion 1.4.4). Users with low vision who rely on browser zoom cannot enlarge the UI. |

---

## Category I: Edge Case Data Bugs

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| I-01 | Tenant with no room assignment shows "Room undefined" in some places | `OwnerPaymentsPage.tsx` | 258–260 | Med | `Room {tenant.assignment?.roomNumber}` — if `tenant.assignment?.roomNumber` is `undefined`, this renders "Room undefined" instead of "Unassigned" or "—". The rooms page and tenants page handle this correctly with `?? "-"` or `?? "Unassigned"` but payments page line 258 has `Room {tenant.assignment?.roomNumber}` without fallback. |
| I-02 | `tenant.email` may be empty string but `t.email.toLowerCase()` called | `OwnerTenantsPage.tsx` | 73 | Low | `TenantRecord.email` is typed as `string` (non-optional). An empty string `""` is valid. `t.email.toLowerCase().includes(query)` works on empty string without crashing, but the phone field `t.phone.toLowerCase()` may crash if `phone` is somehow `null` (typed as `string` but backend could send `null`). |
| I-03 | Tenant with 0 monthly rent causes percentage coverage `NaN` display | `TenantFormModal.tsx` | 176–178 | Med | `paymentCoverage` guard: `Number(form.monthlyRent) > 0 ? ... : 0` — returns `0` when monthlyRent is 0. `0` displays as "0% of first cycle" which is correct. No crash. However the "First Due Preview" still shows a date based on `paidOnDate` which for a ₹0 tenant is meaningless. Minor. |
| I-04 | Hostel with 0 beds/rooms: division by zero in occupancy % | `OwnerDashboardPage.tsx` | 88–89 | Med | `const occ = capacityBase > 0 ? Math.round(...)...  : 0` — guards against division by zero. OK. But `paymentHealthScore = Math.max(0, Math.min(100, occ * 0.4 + cr * 0.6 - overdue.length * 4))` — when `overdue.length` exceeds 25, score goes negative and is clamped to 0. This is expected but shows 0/100 with no explanation. |
| I-05 | Trailing whitespace in room number treated as distinct room | `hostel-occupancy.ts` | 71–76 | Med | `a.roomNumber === room.roomNumber` — exact string match. If a room was created as "101 " (trailing space) and a tenant's assignment has "101" (no space), the tenant won't be counted as occupying that room, making the room appear vacant even when occupied. No `trim()` normalization. |
| I-06 | Two tenants assigned to same bed: both show in room, overcounting | `hostel-occupancy.ts` | 87–99 | High | The `byBedId` map and `noBedTenants` positional matching logic means if two tenants somehow have the same `bedId`, only the first in the map is associated with the bed. The second tenant still appears in `roomTenants` but is not shown in a bed card. The room shows as having a free bed despite being full. |
| I-07 | `tenant.pendingBalance.amount` rendered without checking if `undefined` | `OwnerTenantsPage.tsx` | 238–241 | Low | `tenant.pendingBalance && tenant.pendingBalance.amount.toLocaleString(...)` — `pendingBalance` is checked but `amount` could be `undefined` if the type from the backend is partial. TypeScript says `amount: number` but runtime may differ. |
| I-08 | Very long tenant name breaks mobile card layout | `OwnerTenantsPage.tsx` | 232–234 | Low | `<p className="truncate text-sm font-semibold text-white">{tenant.fullName}</p>` — `truncate` is applied. However the wrapper grid column `1fr` may still overflow on very long names without spaces (e.g. a 50-character name with no spaces). |
| I-09 | Vacating a tenant with no room assignment: misleading "Room Unassigned" text | `VacateTenantModal.tsx` | 119 | Low | `Room {tenant.assignment?.roomNumber ?? "Unassigned"}` — functional but the UI message implies vacating removes a room, confusing for unassigned tenants. |
| I-10 | Payment recorded on future date makes tenant appear paid for months | `payment-collection-modal.tsx` | 74–76 & API | High | As noted in B-03, no `max` on the date input. Beyond UI, the API route (`pay-rent/route.ts` line 58) only validates format (`YYYY-MM-DD`), not whether the date is in the future. A 2030 date creates a `nextDueDate` of 2030+1 month, making the tenant appear "Paid / Active" for 4+ years. |
| I-11 | `fmtTenantId` with undefined | `payment.ts` | 1–3 | Low | `String(undefined).padStart(6,"0")` returns `"undefinedefined"` (actually `"undefined"` padded to 6 = `"undefined"`). If somehow `tenant.tenantId` is `undefined` at runtime (type guard gap), the UI shows "undefined" in the tenant ID field. |

---

## Category J: Network & Error Recovery Bugs

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| J-01 | CSRF token cached in memory: if fetch fails silently, all mutations send empty token | `csrf-client.ts` | 8–20 | High | `cachedToken = ""` is set on CSRF fetch failure (line 17 catch). Subsequent mutations send `x-csrf-token: ""`. The backend may accept this (if validation is lax) or reject all mutations with 403. The user sees only a generic "Unable to …" message with no indication the session expired. |
| J-02 | CSRF token never refreshed after session expiry | `csrf-client.ts` | 6 | High | `cachedToken` is module-level — it persists for the entire browser session. If the server rotates the CSRF token (e.g. on session renewal), the client keeps the old token, causing all mutations to fail until the user hard-reloads. |
| J-03 | `handleRemove` in `VacateTenantModal` not wrapped in try/catch | `VacateTenantModal.tsx` | 57–89 | High | `const { response, data } = await removeTenant(...)` has no try/catch. If `removeTenant` throws (network error, fetch exception), the error propagates uncaught to the React error boundary. The user sees a full-page error, not an inline error message. |
| J-04 | `handleAssign` in `TenantRoomAssignmentModal` not wrapped in try/catch | `TenantRoomAssignmentModal.tsx` | 82–128 | High | `await assignTenantRoom(...)` has no try/catch. Network failure throws to error boundary. |
| J-05 | `handleSave` in `QuickAddTenantModal` has try/catch but only catches generic errors | `QuickAddTenantModal.tsx` | 63–84 | Low | Catch is `catch { setError("Network error. Try again."); }` — does not distinguish between a 400 validation error (which would be caught by the `!response.ok` check) and a network failure. The server error message in `data.message` is used on line 76. OK design but inconsistent with other forms that don't catch at all. |
| J-06 | Submitting form offline: spinner runs forever | All forms | — | High | None of the forms detect offline state (`navigator.onLine`) or set a timeout on fetch. If the user submits while offline, `setSubmitting(true)` is set and the form blocks interaction indefinitely. `fetch()` to a down server will eventually reject (after browser timeout ~30-60 s), triggering the catch, but only if there is a try/catch. Forms without try/catch (J-03, J-04) will never recover. |
| J-07 | Backend HTML error (504, 502) causes `JSON.parse` failure | `OwnerPaymentsPage.tsx` `onSave` | 305 | Med | `await uploadTenantPaymentProof(payload)` returns `{ response, data }`. If the server returns a 504 Gateway Timeout with an HTML body, parsing with `.json()` would fail. The service wrapper may handle this, but no evidence of a `safe-json` parse at the call site here. The `safe-json.ts` library exists but is not used in the service layer for all endpoints. |
| J-08 | `TenantRoomAssignmentModal` hostels fetch has no error handling | `TenantRoomAssignmentModal.tsx` | 52–65 | High | `const { data } = await fetchOwnerHostels()` — if this throws, `setLoading(false)` never runs (it is in the same `load` function, after `fetchOwnerHostels`). The modal shows the loading skeleton (`loading=true`) indefinitely. |
| J-09 | `refreshHostels` in HostelContext silently swallows all errors | `hostel-context.tsx` | 46–73 | High | The try block has no catch — errors propagate to the `finally` which only sets `loading=false`. API errors cause `hostels` to remain at previous state and no UI feedback is given to the user. |
| J-10 | Duplicate tenant creation possible on slow networks (double-click) | All create forms | — | Med | `setSubmitting(true)` prevents double-click, but only if the second click happens after the first click's synchronous state update propagates. A double-click rapid-fire could trigger `handleSave` / `handleSubmit` twice before `submitting=true` is re-rendered, potentially creating duplicate tenants. React batches state updates in event handlers in React 18, so the risk is low but not zero in concurrent mode. |
| J-11 | `PLAYWRIGHT_TEST=true` check bypasses rate limiting in production | `pay-rent/route.ts` | 16 | High | `if (process.env.PLAYWRIGHT_TEST !== "true" && apiRateLimit(...))` — if the production environment variable `PLAYWRIGHT_TEST` is accidentally set to `"true"`, rate limiting is disabled for all payment endpoints. This is a security concern. |

---

## Category K: Mobile-Specific Bugs

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| K-01 | iOS keyboard pushes fixed modal up, obscuring footer buttons | All modals | — | High | When the iOS virtual keyboard opens on a `type="number"` or `type="text"` input inside a modal, the viewport shrinks. Modals using `position:fixed` with `max-h-[92dvh]` or `h-full` will shrink the visible area. The sticky footer (Record Payment, Save Tenant) may be pushed out of the shrunk viewport. `dvh` units help but iOS Safari < 15.4 treats `dvh` = `svh` which is the small viewport height — footer should still be visible but action buttons may be clipped on older iOS. |
| K-02 | `userScalable: false` breaks iOS text resize accessibility | `app/layout.tsx` | 27 | High | (Same as H-14) — On iOS, `user-scalable=no` also prevents the "Larger Text" accessibility setting from working in Safari. |
| K-03 | Long-press on buttons triggers text selection on Android | All clickable text | — | Low | Buttons with text labels (e.g. "Collect Rent", "Add Tenant") do not have `user-select: none` CSS. On Android Chrome, a long-press selects the button text, which is annoying but not functionally blocking. |
| K-04 | Mobile virtualizer in OwnerTenantsPage uses `window.scrollY` offset but iOS may return decimal | `OwnerTenantsPage.tsx` | 101–106 | Low | `useWindowVirtualizer` with `scrollMargin: mobileScrollMargin` where `mobileScrollMargin = mobileListRef.current.offsetTop`. On high-DPI iOS screens, `offsetTop` may return a sub-pixel decimal value. React Virtual expects integer pixel values. |
| K-05 | Touch target for Remove-receipt X button is only ~20 px × 20 px | `TenantFormModal.tsx` | 804–810 | Med | `<button className="absolute -right-2 -top-2 rounded-full bg-red-500 p-1">` — `p-1` = 4 px padding, icon `h-3 w-3` = 12 px. Total touch target ≈ 20 px, well below the recommended 44 px minimum. |
| K-06 | Same small target for remove-receipt in payment-collection-modal | `payment-collection-modal.tsx` | 266–270 | Med | `p-1` + `h-3 w-3` icon = ~20 px touch target. |
| K-07 | "Missing info" badge button in tenant list has small touch target | `OwnerTenantsPage.tsx` | 243–251 | Med | `px-2 py-0.5` = ~24 px height. Below 44 px. On mobile, this small orange badge is hard to tap accurately. |
| K-08 | Mobile number input (type=number) shows decimal keyboard on iOS | All monetary inputs | — | Low | `<input type="number" min="0">` — on iOS, this shows the standard number keyboard with decimal point, which is fine. However, combined with B-01 (allowing "e"), the iOS keyboard also has the "." key, allowing `1.5e3`. |
| K-09 | `WebkitOverflowScrolling: "touch"` is deprecated | Multiple modals | — | Low | `-webkit-overflow-scrolling: touch` was deprecated in iOS 13+. Modern iOS uses native momentum scrolling by default. The property is harmless but adds dead CSS. |
| K-10 | Notch / Dynamic Island: modal close buttons at top-right may be hidden | `TenantFormModal.tsx` | 374 | Low | On iPhone 14 Pro+ with Dynamic Island, the `pt-4` padding at the top of the modal header (`pt-4 sm:pt-5`) may place the close button behind the Dynamic Island cutout (usually handled by `env(safe-area-inset-top)` which is set via `viewportFit: "cover"`). The close button is at `absolute right` positioning in the header. If `safe-area-inset-top` is not applied to the modal overlay, the X button overlaps the Dynamic Island. |
| K-11 | `TenantRoomAssignmentModal` in mobile mode: `min-h-[88dvh]` forces nearly full screen | `TenantRoomAssignmentModal.tsx` | 165 | Low | `min-h-[88dvh]` on the Card means even for a hostel with 2 rooms, the modal is almost full-screen height. Small-content modals should be `height:auto`, not forced to 88dvh minimum. |

---

## Category L: Demo Mode & Data Integrity Bugs

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| L-01 | `PLAYWRIGHT_TEST=true` in production disables rate limiting | `pay-rent/route.ts` | 16 | High | (Same as J-11) — Critical security concern if env var is accidentally set. |
| L-02 | Demo tenant data in `getTenantRecords` is shared in-process (no user isolation) | `OwnerTenantDetailsPage.tsx` | 34 | High | `getTenantRecords(session.isDemo)` reads from an in-process store. If multiple demo users are accessing the server simultaneously (same Next.js process), they share the same demo data object. A mutation by one demo user affects all other demo sessions. |
| L-03 | Demo mode: tenants page scopes by `currentHostel.id` but demo hostelId may mismatch | `OwnerTenantsPage.tsx` | 55–59 | Med | `allTenants.filter(t => t.hostelId === currentHostel.id || t.assignment?.hostelId === currentHostel.id)` — demo tenants may have a fixed `hostelId` that does not match the currently selected hostel ID if the demo hostel was re-created. This would show 0 tenants on the demo tenants page. |
| L-04 | Draft restored for demo user from localStorage despite being a different session | `TenantFormModal.tsx` | 99–113 | Med | The draft key `"tenant-form-draft-v5"` is global (not scoped to the demo user or owner ID). If a live owner and a demo owner share the same browser profile (unlikely but possible on shared computers), the draft from one is loaded by the other. |
| L-05 | `RemoveTenantSearch` after success calls `router.refresh()` but in demo mode this may not update in-process store | `RemoveTenantSearch.tsx` | 84 | Med | `router.refresh()` triggers a server-side re-render which reads from `getTenantRecords()`. The demo store mutation runs on `removeTenant()` call. If the refresh races with the mutation, the page might re-render before the in-process store is updated, showing the deleted tenant again. |
| L-06 | Draft `localStorage` key `"tenant-form-draft-v5"` not cleared on logout | All | — | Med | When the owner logs out, `resetFormState()` is only called when the modal is explicitly closed. If the owner navigates away or closes the tab, the draft persists. Next login (even a different owner on the same browser) loads this draft. |
| L-07 | `hostel-context.tsx` stores `currentHostelId` in `localStorage` without owner-scoping | `hostel-context.tsx` | 17–18 | Med | `STORAGE_KEY = "currentHostelId"` is a plain key. If owner A logs out and owner B logs in on the same browser, the stored hostel ID from owner A is loaded. `refreshHostels` then tries to find that hostel in owner B's list — it won't match, so `nextHostels[0]` is used as fallback. This is safe but may confuse owner B who expects to see their primary hostel. |

---

## Category M: API Route Bugs (Extended Pass)

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| M-01 | `paidOnDate` not validated for future dates in `/api/tenants/pay-rent` | `pay-rent/route.ts` | 58–60 | High | Route validates date format (`YYYY-MM-DD`) but not whether the date is ≤ today. A future `paidOnDate` of 2030 generates a `nextDueDate` of 2030+1month, making the tenant appear "Active / Paid" for years. |
| M-02 | `paidOnDate` not validated for future dates in `/api/tenants` POST | `tenants/route.ts` | 177–179 | High | Same gap: format-only validation. Creating a tenant with `paidOnDate: "2030-01-01"` makes them appear paid far into the future from day one. |
| M-03 | Initial payment in live `POST /api/tenants` hardcodes `paymentMethod: "cash"` | `tenants/route.ts` | 225 | Med | The first payment history entry always uses `"cash"` regardless of how the tenant actually paid at admission. For tenants who transferred via UPI, the audit trail is incorrect. |
| M-04 | `/api/tenants GET` returns `data` mirror wrapper only for non-live mode | `tenants/route.ts` | 67 | Low | `return { ...sliced, data: sliced }` — the `data` field is added for test-suite compatibility but only in demo/local mode. Live mode returns the raw tenant object. Any client code that reads `.data.someField` instead of direct `.someField` will silently fail in live mode. |
| M-05 | `idempotencyCache` eviction removes only one key at capacity | `tenants/route.ts` | 93–96 | Low | When `_idempotencyCache.size >= _IDEMPOTENCY_MAX` (500), only one entry is deleted (the first key). Under high burst load the cache can still temporarily exceed 500 entries since multiple requests may hit the capacity check concurrently in a single-threaded event loop. |
| M-06 | Restore route checks `content-length` but Node.js may buffer full body first | `backup/restore/route.ts` | 23–25 | Low | `content-length` header check happens before `request.json()`. In Node.js, the body is already buffered in memory by the time the route handler runs. The 10 MB guard effectively happens too late to prevent OOM from a 500 MB upload — it only prevents parsing. |
| M-07 | `/api/tenants/remove` does not validate `settlementDate` format | `remove/route.ts` | 27–35 | Med | The route accepts `settlementDate` as an optional string and passes it directly to the ledger entry without validating it is `YYYY-MM-DD`. An invalid date (e.g. "yesterday") creates a ledger entry with `date: "yesterday"` which breaks any date-based sorting or filtering. |
| M-08 | `PLAYWRIGHT_TEST` check in `/api/tenants/route.ts` enables rate limit bypass on tenant creation | `tenants/route.ts` | 104 | High | Same pattern as J-11 and L-01. If env var accidentally set in production, any user can create unlimited tenants with no throttle. |
| M-09 | `PLAYWRIGHT_TEST` check in all mutation routes (assign-room, family-members, payment-proof, remove, upload-document) | Multiple API routes | — | High | The same `process.env.PLAYWRIGHT_TEST !== "true"` bypass exists in at least 7 mutation routes. Single env var misconfiguration disables rate limiting across the entire API. |
| M-10 | `/api/tenants/assign-room` does not re-validate that room is not full before assigning in live mode | `assign-room/route.ts` | 43–78 | High | In live mode, the route fetches the tenant and calls the backend PUT immediately without checking if the chosen room's bed is already taken. The demo path (`assignTenantRoom` in tenantStore) performs the bed availability check, but the live path relies entirely on the backend to reject — if the backend is permissive, double-booking is possible. |
| M-11 | `/api/owner-hostels POST` creates `draftId` using `Date.now()` which is not unique under load | `owner-hostels/route.ts` | 72 | Low | `const draftId = \`draft-${Date.now()}\`` — two simultaneous hostel creation requests in the same millisecond (possible under load) produce the same `draftId`, which then generates the same room/bed IDs. This can cause bed ID collisions. |
| M-12 | `/api/owner-hostels GET` filters out occupied beds but returns room list with `rooms[]` containing only free beds | `owner-hostels/route.ts` | 36–44 | Med | The response filters beds: `beds: (room.beds ?? []).filter((bed) => !bed.occupied)`. This means the room list returned by this endpoint is missing occupied beds. When `TenantRoomAssignmentModal` uses this endpoint to show available rooms, it cannot display which beds are already taken — it only sees the free ones. This misleads the owner about actual room state. |
| M-13 | Finance ledger GET has no rate limiting | `finance-ledger/route.ts` | 7 | Low | No `apiRateLimit` call — the endpoint can be queried at unlimited frequency. With large ledgers this could be used for DoS by hammering the in-process store. |
| M-14 | `/api/complaints` GET endpoint proxies to backend but drops request errors silently | `complaints/route.ts` | 15 | Med | `const res = await backendFetch('/api/complaints/${qs}')` — if the backend fails with a network error, `backendFetch` re-throws. The catch block for the route does not exist. An unhandled rejection surfaces as a 500 with `Internal Server Error` and no meaningful message to the client. |
| M-15 | `/api/csrf GET` has no rate limiting | `csrf/route.ts` | 8 | Low | The CSRF endpoint can be queried at unlimited frequency — useful for a token enumeration attack if the CSRF validation is weak. Low risk given HttpOnly cookie comparison, but consistent omission of rate limiting on public-facing endpoints. |
| M-16 | `/api/owner/profile PATCH` missing `dynamic = "force-dynamic"` declaration position | `owner/profile/route.ts` | 51 | Low | `export const dynamic = "force-dynamic"` appears at line 51 — after the `PATCH` handler and GET handler definitions. While Next.js picks this up at module initialization, convention dictates it should be at the top of the file to avoid confusion and potential caching surprises if Next.js changes evaluation order. |
| M-17 | Upload route joins path segments with `-` instead of `/` | `uploads/[...path]/route.ts` | 48 | Med | `const filename = rest.join("-")` — if the path was `/api/uploads/payment-proofs/abc/def.jpg`, `rest = ["abc", "def.jpg"]` → `filename = "abc-def.jpg"`. But files are saved with format `${tenantId}-${Date.now()}-${sanitizedName}` (no subdirectory). The join logic is correct for the current save pattern but fragile: a filename containing a `-` could be mis-constructed. |
| M-18 | `/api/tenants/upload-document` misses `dynamic = "force-dynamic"` | `upload-document/route.ts` | — | Low | No `export const dynamic = "force-dynamic"` — the route may be statically cached by Next.js optimizer, causing uploads to fail silently in some deployment configurations. |
| M-19 | `backendFetch` does not include a `Refresh-Token` rotation flow | `backend-api.ts` | 7–38 | High | When the `access_token` cookie is expired, `backendFetch` sends a 401-causing request, returns the 401 response, and the caller returns an error to the user. There is no token refresh mechanism: the user is stuck in a 401 loop until they hard-reload or log in again. UX for live mode owners who close their laptop for >7 days is: all API calls silently fail. |
| M-20 | `backendFetch` `timeout` value `BACKEND_TIMEOUT_MS` defaults to 8000 ms — too short for file uploads | `backend-api.ts` | 5 | Med | 8 seconds is the default timeout for all backend requests including file uploads (proof images up to 5 MB). On slow connections, a 5 MB upload will abort with a 504 and a confusing "server is taking too long" message. |
| M-21 | `setAuthCookies` uses `Max-Age: 604800` (7 days) for both access and refresh tokens | `backend-api.ts` | 43–46 | Med | Setting both tokens to the same 7-day expiry means the refresh token cannot be used to renew an expired access token — they both expire simultaneously. A proper implementation would give the refresh token a longer lifetime (e.g. 30 days). |
| M-22 | `clearAuthCookies` sends `Max-Age=0` without `Path=/ SameSite=Lax` inconsistency risk | `backend-api.ts` | 52–57 | Low | The `Secure` attribute in `clearAuthCookies` is conditionally added, but cookies are cleared with the same path/samesite as they were set. This is correct — but if the production deploy ever uses a subdomain, the `SameSite=Lax` without `Domain=` may fail to clear the cookie on the parent domain. |
| M-23 | CSV export does not handle `t.createdAt` being undefined | `tenants/export/route.ts` | 57 | Med | `t.createdAt.slice(0, 10)` — if a backend tenant record is missing `createdAt`, this throws `TypeError: Cannot read properties of undefined (reading 'slice')`. No optional chaining. |
| M-24 | CSV formula injection neutralization misses tab character in cell value | `tenants/export/route.ts` | 14–18 | Med | `const safe = /^[=+\-@\t\r]/.test(str) ? \`'${str}\` : str` — the `\t` check catches tab at start but not embedded tabs inside a non-leading position. A tenant name like `"Normal\tName"` is not sanitized and may break some spreadsheet parsers. |
| M-25 | Export endpoint has no authentication rate limiting | `tenants/export/route.ts` | 66 | Low | Only `requireOwnerSession` auth check, no `apiRateLimit`. A session holder can hammer the export endpoint, generating large CSV responses that consume server CPU. |
| M-26 | `validatePaymentProofWithMagicBytes` does not handle HEIC for payment proofs | `document-upload.ts` | 70–83 | Low | `PROOF_ALLOWED_MIME_TYPES` does not include `image/heic` or `image/heif`. However, the upload-document endpoint accepts HEIC for tenant photos. An iOS camera photo (HEIC) cannot be used as payment proof, which is counter-intuitive for Indian users on iPhones. |
| M-27 | `savePaymentProofImage` fallback stores base64 data URL in tenant record | `payment-proof-upload.ts` | 43–51 | High | When the filesystem is read-only (Vercel deployment), the file is stored as a `data:image/jpeg;base64,...` string in the `proofImageUrl` field. For a 5 MB proof image, this is a ~6.7 MB base64 string stored in the tenant's JSON record. This bloats the in-memory store, slows all subsequent JSON serialization, and can cause the `tenants.json` file to exceed the 10 MB restore limit. |
| M-28 | `saveTenantDocument` same base64 fallback — no size limit for the base64 path | `document-upload.ts` | 93–113 | High | The filesystem-write attempt may fail silently and fall back to base64. The 5 MB size check at the start of `validateDocumentFileWithMagicBytes` is done before calling `saveTenantDocument`. But the document upload route validates size first, so it's safe here — unless `saveTenantDocument` is called directly from other code without prior validation. |
| M-29 | `sanitizeFileName` in payment-proof-upload uses `.slice(0, 40)` but document-upload uses `.slice(0, 30)` | Both files | ~6 | Low | Inconsistent filename length limits across the two upload functions. Not a bug per se but a maintenance inconsistency that could cause issues if filename length constraints change in the future. |
| M-30 | `maybeAutoBackup` checks `today.replace(/-/g, "-")` which is a no-op | `backup.ts` | 107 | Low | `const today = new Date().toISOString().slice(0, 10)` produces `"2026-06-10"`. `today.replace(/-/g, "-")` replaces dashes with dashes — no change. The backup check then compares file prefixes using the unchanged string. The auto-backup deduplication logic works by coincidence but the replace is dead code. |

---

## Category N: Data Store Bugs (Extended Pass)

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| N-01 | `createTenantRecord` hardcodes `hostelId: DEMO_OWNER_HOSTEL_ID` for all new tenants | `tenantStore.ts` | 403 | High | Every tenant created via the demo path gets `hostelId: DEMO_OWNER_HOSTEL_ID` regardless of which hostel was actually selected. The `hostelId` at the top-level of `TenantRecord` is set from a constant, not from the `assignment.hostelId`. This means `getTenantRecords().filter(t => t.hostelId === selectedHostelId)` for non-default hostels returns empty. |
| N-02 | `generateTenantIdFromPhone` uses last 5 digits of phone — collisions with existing tenants | `tenantStore.ts` | 344–353 | Med | Tenant IDs are derived from phone numbers. Two tenants with phones ending in the same 5 digits (e.g. `9876501234` and `+91 9856501234` → same last 5 = `01234`) would collide. The `records.some(t => t.tenantId === preferredId)` check prevents true duplicates but falls back to a random 5-digit ID, which may cause confusion when the owner references a tenant by phone. |
| N-03 | `recordTenantPayment` discount `discountApplied` can exceed `monthlyRent` for percent discounts | `tenantStore.ts` | 621–631 | Med | `discountApplied = Math.round((tenant.monthlyRent * disc.value) / 100)` — if `disc.value > 100` (no validation on the discount percent in the API route `pay-rent/route.ts`), the discount exceeds the rent. `balanceAmount = Math.max(0, effectiveRent - amount)` where `effectiveRent = monthlyRent - discountApplied` could go negative. `Math.max(0, ...)` clamps it, but `balanceAmount = 0` means no balance is recorded, silently creating a "free rent" situation. |
| N-04 | `discountValue` has no upper bound validation in `extractPayOptions` | `pay-rent/route.ts` | 154–166 | Med | `discountValue = Math.max(0, Number(...))` — no maximum. An owner can accidentally enter 10000 as a percent discount (10000%), creating a negative effective rent. The `Math.max(0, ...)` on `effectiveRent` prevents negative values but silently zeroes the rent. |
| N-05 | `isBalanceCollection` check uses `tenant.pendingBalance` but does not verify the balance amount matches | `tenantStore.ts` | 575–603 | Med | When collecting a deferred balance, the route accepts any `amount` value and applies it. If the `amount` is greater than `pendingBalance.amount`, the excess is silently accepted — the owner effectively over-collects. No validation that `amount <= pendingBalance.amount`. |
| N-06 | `assignTenantRoom` in demo mode checks `currentActiveAssignment` for same-bed re-assign — throws error on re-assignment | `tenantStore.ts` | 454–461 | Med | If a tenant is already assigned to a room and the owner tries to re-assign to the same room and bed, the check `currentActiveAssignment.hostelId !== assignment.hostelId || ...bedId !== assignment.bedId` evaluates to false (they match) — so re-assign to same room/bed succeeds. But if the owner tries to correct a typo in sharingType while keeping same bed, the throw fires: "Tenant already has an active allocation." There is no "clear before reassign" UI flow to guide the owner. |
| N-07 | Payment history cap at 120 entries silently drops oldest history | `tenantStore.ts` | 700–703 | Med | `if (tenant.paymentHistory.length > 120) tenant.paymentHistory = tenant.paymentHistory.slice(0, 120)` — the oldest entries (> 120 months of history = 10+ year tenants) are permanently deleted without warning. The owner loses payment audit trail for long-term tenants. |
| N-08 | `persistTenantRecords` calls `maybeAutoBackup()` which reads from disk synchronously on every write | `tenantStore.ts` | 313–321 | Med | Every payment, assignment, or profile update triggers `maybeAutoBackup()` which calls `fs.readdirSync(BACKUP_DIR)`. Under concurrent load, multiple synchronous directory reads on every mutation add I/O latency to the request. |
| N-09 | `loadTenantRecords` falls back to demo data if file parse fails and overwrites the file | `tenantStore.ts` | 291–310 | High | If `tenants.json` is temporarily corrupt (e.g. partial write), `JSON.parse` throws, the catch returns `getDemoTenantRecords()`, and `persistTenantRecords(demo)` immediately overwrites the real data with demo data. The actual owner data is lost. No backup is created before overwriting. |
| N-10 | `seedDemoTenantsForHostel` uses `formatDateFromToday` for `nextDueDate` rather than `calculateNextDueDate` | `tenantStore.ts` | 859 | Med | `const nextDueDate = formatDateFromToday(profile.dueOffsetDays)` — this directly adds a day offset to today. Unlike `calculateNextDueDate`, it ignores the billing anchor date. For weekly/daily billing cycle tenants, the resulting `nextDueDate` may not align with the anchor date cadence. |
| N-11 | `getTenantRecordById` returns `undefined` when not found — callers may not handle this | `tenantStore.ts` | 369–372 | Med | `records.find(...)` returns `undefined` if not found. The caller in `remove/route.ts` at line 90 does `const currentTenant = getTenantRecordById(...)` and then uses `currentTenant?.fullName ?? "Tenant"`. OK for optional chaining, but if code elsewhere calls the function without optional chaining, a crash results. |
| N-12 | `reloadTenantRecords` modifies the array in-place with `splice` — race condition if read concurrently | `tenantStore.ts` | 323–327 | Med | `tenantRecords.splice(0, tenantRecords.length, ...fresh)` — while this is running, another concurrent request (Node.js event loop) may be iterating over `tenantRecords`. Depending on the JS engine's implementation of `splice`, the array may be in an inconsistent state during the splice operation. |
| N-13 | Demo tenant `billingCycle` is only set for tenant `51211` — rest default to undefined (monthly) | `tenantStore.ts` | 224 | Low | Only `51211` has explicit `billingCycle: "daily" as const`. The rest use `buildDemoTenant` which does not set `billingCycle`. When `getDueStatus(tenant.nextDueDate, tenant.billingCycle)` is called, `billingCycle` is `undefined`, defaulting to `"monthly"`. This is the correct behavior but is implicit. |
| N-14 | `updateTenantProfile` recalculates `nextDueDate` when `billingCycle` changes but not when `paidOnDate` changes | `tenantStore.ts` | 736–743 | Med | If an owner corrects a tenant's `paidOnDate` via the PATCH endpoint, the `nextDueDate` is NOT recalculated (the condition only checks `patch.billingCycle || patch.monthlyRent !== undefined`). This leaves `nextDueDate` stale, causing incorrect due date display. |

---

## Category O: UI/Page-Level Bugs (Extended Pass)

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| O-01 | `OwnerPaymentsPage` "Amount" column shows `tenant.rentPaid` (last payment) not cycle total | `OwnerPaymentsPage.tsx` | 150, 219 | Med | `tenant.rentPaid` is the amount of the LAST payment, not the total collected for the current cycle or the full monthly rent. For a partially-paid tenant, this shows a misleading lower amount. Column header says "Amount" with no clarifying label. |
| O-02 | `OwnerPaymentsPage` `collectedTotal` uses `rentPaid` for active tenants — does not account for partial payments | `OwnerPaymentsPage.tsx` | 74–78 | Med | `collectedTotal = dueItems.filter(status.tone === "green").reduce(sum + tenant.rentPaid, 0)` — for a tenant who paid partial rent (so `nextDueDate` is the same, `tone !== "green"`), they are excluded from `collectedTotal`. But the partial-paid tenant's `rentPaid` also appears as 0 in the Collected stat. This is arguably correct behavior but counter-intuitive. |
| O-03 | `AddProofModal` in `OwnerPaymentsPage` has no file size validation | `OwnerPaymentsPage.tsx` | 487–560 | Med | The `AddProofModal` `<input type="file">` accepts any file. The client-side renders the image preview without size checks. A 50 MB file would be appended to the `FormData` and attempted to upload (the server would reject it, but the UX shows no client-side warning). |
| O-04 | `AddProofModal` uses `createPortal` check but renders inline (not portal) | `OwnerPaymentsPage.tsx` | 273 | Low | `{proofModal && typeof window !== "undefined" ? <AddProofModal ...> : null}` — checked that `window` is defined (SSR guard) but the modal renders as an inline child of `OwnerPaymentsPage`, not via `createPortal`. This means it is inside the scrollable page flow and may be partially hidden by the page layout. |
| O-05 | `OwnerCreateHostelPage` `hostelType` state is hardcoded to `"PG"` and cannot change | `OwnerCreateHostelPage.tsx` | 80 | Med | `const [hostelType] = useState<"PG" | "RESIDENCE">("PG")` — the setter is never destructured or exposed in the UI. The hostel type toggle was removed from the UI but the state remains. All new hostels created through this page are forced to type "PG". If a RESIDENCE owner creates a hostel here, it is created as PG. |
| O-06 | `OwnerCreateHostelPage` bed setup allows tenant names without phone numbers | `OwnerCreateHostelPage.tsx` | 263 | Low | `phone: bed.phone.trim() || undefined` — phone is optional. A tenant created at hostel setup has no phone, which is fine functionally, but the tenant will fail validation in any place that expects `phone` to be present (e.g. `generateTenantIdFromPhone` falls back to random when phone is empty). |
| O-07 | `OwnerCreateHostelPage` sends all bed tenants concurrently via `Promise.allSettled` with no concurrency cap | `OwnerCreateHostelPage.tsx` | 286 | Low | If a hostel has 100 beds, 100 `POST /api/tenants` requests fire simultaneously. This saturates the event loop, may exceed the rate limiter (60/min), and causes server-side data race conditions in the in-process tenant store. |
| O-08 | `OwnerCreateHostelPage` `handlePlanSelect` calls `requestOwnerPlanUpgrade` but ignores the response | `OwnerCreateHostelPage.tsx` | 310–320 | Med | `await requestOwnerPlanUpgrade(...)` is awaited inside `try` with no check on the response. If the upgrade request fails (403, 500), `setSubmittingPlanId(null)` fires in `finally`, the loading state stops, and `router.replace("/owner/dashboard")` still runs — the owner navigates to the dashboard with no plan upgrade and no error message. |
| O-09 | `OwnerReportsPage` finance ledger fetch uses `fetch` (not `csrfFetch`) but no CSRF needed for GET | `OwnerReportsPage.tsx` | 26 | Low | `fetch('/api/finance-ledger?...')` — GET request, no CSRF token needed, correct. But no error boundary or `loading` state for the ledger. If the request fails, `ledgerEntries` stays as `[]` silently — the "Advances" and "Service Fees" totals show Rs 0 with no indication of an error. |
| O-10 | `OwnerBackupPage` restore from file has no file size check before `file.text()` | `OwnerBackupPage.tsx` | 114–137 | Med | `const text = await file.text()` reads the entire file into memory with no size check. A 500 MB file would load into browser memory and then be serialized into a `fetch` body. The server has a 10 MB `content-length` check but the client has no guard, causing browser memory issues. |
| O-11 | `OwnerBackupPage` restore success message says "Reload the page to see updated data" but doesn't auto-reload | `OwnerBackupPage.tsx` | 102 | Low | After a successful restore, the message tells the user to reload manually. No `window.location.reload()` or `router.refresh()` call. The page still shows old data until the user manually reloads. |
| O-12 | `OwnerComplaintsPage` QrModal canvas renders using `window.location.origin` — shows wrong URL in SSR | `OwnerComplaintsPage.tsx` | 17 | Low | `const url = typeof window !== "undefined" ? \`${window.location.origin}/c/${hostelId}\` : \`/c/${hostelId}\`` — the SSR fallback `/c/${hostelId}` is a relative URL and would generate a QR code pointing to a relative path if somehow rendered server-side. `useEffect` properly uses `window.location.origin` for the canvas, so actual QR code is correct. But if `dataUrl` is used outside a client, it has the wrong URL. |
| O-13 | `TenantFamilyMembersModal` does not pre-populate existing family members on open | `TenantFamilyMembersModal.tsx` | 38 | Med | `const [members, setMembers] = useState([createFamilyMember()])` — always initializes with one blank member. If a tenant already has family members (from a previous save), re-opening the modal shows a blank form instead of the existing data. The owner cannot edit existing members without re-entering everyone. |
| O-14 | `TenantFamilyMembersModal` error div has no `role="alert"` | `TenantFamilyMembersModal.tsx` | ~90 | Med | `{error ? <p className="...text-red-400">{error}</p> : null}` — no `role="alert"` attribute. Screen readers won't announce the error. |
| O-15 | `OwnerSettingsPage` is a server component but calls `getOwnerHostel(undefined, ...)` — undefined hostelId | `OwnerSettingsPage.tsx` | 28 | Med | `hostel = getOwnerHostel(undefined, session.isDemo)` — `getOwnerHostel` with `undefined` as the first argument returns the first hostel in the store. If the owner has multiple hostels, this always returns the first one, not necessarily the "current" hostel they're working on. The settings page shows the first hostel regardless of which hostel is selected. |
| O-16 | `OwnerDashboardPage` `paymentHealthScore` formula deducts 4 points per overdue tenant — breaks for large hostels | `OwnerDashboardPage.tsx` | 90 | Low | `phs = Math.max(0, Math.min(100, occ * 0.4 + cr * 0.6 - overdue.length * 4))`. A hostel with 10 overdue tenants gets -40 subtracted, meaning 100% occupancy + 100% collection rate (= 100 score) drops to 60 before clamping. This is reasonable. But for a large hostel with 30 overdue tenants, the deduction is 120 — clamped to 0. The score reads as 0/100 even if occupancy is 99% and only 30% of 200 tenants are overdue. |
| O-17 | `OwnerDashboardPage` redirect race condition: `hostelLoading=false` fires before first hostel loads | `OwnerDashboardPage.tsx` | 27–31 | High | `if (!hostelLoading && !currentHostel) router.replace("/owner/create-hostel")`. On first load, `hostelLoading` starts as `false` (before the fetch begins), so `currentHostel=null` briefly triggers the redirect before the hostel fetch completes. The `HostelContext` sets `loading=true` synchronously in the first `useEffect`, but if the effect hasn't fired yet (first paint), there's a brief window where `hostelLoading=false && currentHostel=null`. |
| O-18 | `OwnerDashboardPage` `allDueItems` useMemo recalculates on every render — no dep array optimization | `OwnerDashboardPage.tsx` | 96–101 | Low | `useMemo(() => tenants.map(...), [tenants])` — `tenants` is derived from another `useMemo` that depends on `allTenants` and `hostel.id`. Both are stable, so this is fine. But the chain of 6 `useMemo` calls for derived data creates a long dependency chain that re-evaluates entirely when `allTenants` changes (e.g. on a 20s stale timer refetch). |
| O-19 | `OwnerReportsPage` loading state missing — `loading` from `useOwnerTenants` not used | `OwnerReportsPage.tsx` | 15, 70 | Med | `const { tenants: allTenants, loading } = useOwnerTenants(...)` — `loading` is destructured but never shown as a loading state in the JSX. The page renders immediately with potentially empty `allTenants = []`, shows Rs 0 for all stats, then updates silently when data arrives. No skeleton or loading indicator. |
| O-20 | `OwnerReportsPage` `grossIncome` adds `advanceCollected` to income — advance is refundable liability | `OwnerReportsPage.tsx` | 63 | Med | `const grossIncome = collectedRent + advanceCollected + serviceFeeCollected` — advance deposits are refundable. Including them in "gross income" is an accounting error. A more correct metric would be `collectedRent + serviceFeeCollected` (non-refundable). The page misleads owners into thinking advance deposits are earned income. |
| O-21 | Complaint toggle button in `OwnerComplaintsPage` uses `csrfFetch` but error handling minimal | `OwnerComplaintsPage.tsx` | ~200 | Med | The toggle request error is not exposed in the UI — only a console error (if any). If toggling complaints fails, the switch appears to toggle but actually didn't persist. |
| O-22 | `OwnerComplaintsPage` QrModal close button has no `aria-label` | `OwnerComplaintsPage.tsx` | 45 | Low | `<button type="button" onClick={onClose} className="...">` — contains only an `<X>` icon, no `aria-label`. Screen reader announces "button" only. |
| O-23 | `OwnerBillingPage` component not audited — unknown bugs possible | `OwnerBillingPage.tsx` | — | Low | This file was not read during the audit. Billing pages often have complex state management and payment flow logic that may contain additional bugs. |

---

## Category P: Auth & Session Bugs (Extended Pass)

| # | Bug | File | Line | Severity | Description |
|---|-----|------|------|----------|-------------|
| P-01 | `verifyJwtPayload` returns `null` when `JWT_SECRET` env var is not set | `auth.ts` | 19–21 | High | `if (!raw) return null` — if `JWT_SECRET` is not set in the environment, ALL tokens verify as `null`, and `getOwnerSession()` returns `mode: "guest"` for every request. Every auth-required route becomes a 401 or redirects to login. No startup warning is emitted. |
| P-02 | `decodeJwtPayload` does not verify signature — can be used to bypass auth checks if called directly | `auth.ts` | 8–17 | High | `decodeJwtPayload` decodes the payload without verifying the JWT signature. If any code path uses `decodeJwtPayload` instead of `verifyJwtPayload` for auth decisions, tokens can be forged. Current codebase uses `verifyJwtPayload` for auth — risk is in future code reuse. |
| P-03 | `getOwnerSession` does not refresh the access token when expired | `session-mode.ts` | 6–39 | High | `verifyJwtPayload` returns `null` for expired tokens. `session.mode` becomes `"guest"`, triggering a 401 response. No automatic token refresh flow exists. Live mode owners who idle >7 days (token TTL) are logged out silently on the next request. |
| P-04 | Demo token `signDemoToken` is signed with the same `JWT_SECRET` as live tokens | `sign-token.ts` (referenced) | — | Med | If an attacker extracts a demo JWT and the server's signing key is compromised, they can forge live owner tokens with the same format. There is no demo-specific signing key or token audience claim that would prevent a demo token from being validated as a live token. (Mitigation: `session.isDemo` check on `payload.source`.) |
| P-05 | `authRateLimit` uses shared `store` map with `apiRateLimit` — different limits on same key namespace | `rate-limit.ts` | 47–50 | Low | Both `authRateLimit` and `apiRateLimit` use the same `store: Map<string, Entry>`. With `TRUST_PROXY_HEADERS=false` (default), all IPs map to `"unknown"`. Auth limit is 10/15min, API limit is 60/1min. The same `"unknown"` key is used for both — but since they check different `Entry` objects... wait: they share the SAME `store`. An `authRateLimit("unknown")` call and an `apiRateLimit("unknown")` call both write to `store.get("unknown")`. Whichever runs first sets the entry; the second creates a collision. **The counters are shared between auth and API limits for the same IP key.** This means 60 API requests can count toward the 10-attempt auth limit. |
| P-06 | `authRateLimit` window is 15 minutes but entry expiry uses `resetAt` that is per-window — not sliding window | `rate-limit.ts` | 34–43 | Low | The rate limiter uses a fixed window (count resets at `resetAt`). An attacker can try 10 times at the end of window 1, then try 10 times immediately at the start of window 2 — effectively getting 20 attempts in a short period spanning the window boundary. |
| P-07 | Owner signup route (`/api/auth/register`) does not validate password strength beyond min length of 0 | `auth/register/route.ts` | — | Med | The registration endpoint passes `password` directly to the backend. The Next.js BFF layer has no minimum password length check. If the backend accepts empty passwords, an owner can register with `password: ""`. The `change-password` endpoint enforces 8 chars but registration does not. |
| P-08 | Owner login route returns 503 for backend connection failure — no retry hint for user | `auth/owner/login/route.ts` | 49–51 | Low | `catch { return NextResponse.json({ message: "Unable to sign in right now..." }, { status: 503 })` — returns 503 but the frontend login form may show this as a generic "incorrect password" error rather than "server is down". |
| P-09 | `demo-login` route skips rate limiting entirely in test environment | `auth/demo-login/route.ts` | 12–14 | Med | `if (process.env.PLAYWRIGHT_TEST !== "true") { authRateLimit(ip) }` — rate limiting is skipped for demo-login when `PLAYWRIGHT_TEST=true`. In production, an attacker who knows the env var setting can flood demo-login requests. Low risk since demo tokens are low-privilege, but consistent policy is better. |
| P-10 | `session-mode.ts` does not check token expiry explicitly — relies on `jwtVerify` | `session-mode.ts` | 7 | Low | `verifyJwtPayload` uses `jwtVerify` which validates expiry by default. This is correct. However the function returns `null` for both expired tokens and invalid signatures — the caller cannot distinguish "expired" from "tampered". The UX for both cases is the same (logged out), but logging and monitoring benefit from distinguishing them. |

---

## Notes on Categories Not Found in Code

The following bugs from the specification were checked and **not confirmed** in the actual code:

- **"Paid" status badge is a non-interactive `<span>`** — confirmed as correct (intentional, not a button).
- **`formatPaymentDate` crashes on null** — actually handled: `if (!value) return "—"` at line 37 of `payment.ts`.
- **Empty state missing on tenants page** — actually implemented at line 178–200 of `OwnerTenantsPage.tsx`.
- **`useOwnerTenants` has no staleTime** — incorrect; staleTime is 20,000 ms (line 15 of `use-owner-tenants.ts`).
- **Button loading state missing** — `Button` component has `loading` prop that sets `aria-busy` and `disabled`.
- **`/api/tenants/assign-room` missing sharingType validation** — confirmed: line 39 sets `body.sharingType = ""` as default, not rejected.
- **`backendFetch` does not handle non-JSON responses** — partially true: it always calls `.json()` on the response. If backend returns HTML (502/504), `.json()` throws and the route catches it as a 504. Handled.

---

*End of Bug Catalog — 312 bugs total*
